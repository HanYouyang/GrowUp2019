const config = {
    hit_offset: {
        _comment: '子弹偏移目标的距离',
        value: 20,
        min: -50,
        max: 500,
    },
    pipe_space_test: {
        _comment: '测试状态：两个管子垂直方向的距离',
        value: 400,
    },
}
